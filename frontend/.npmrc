# 忽略所有安装脚本（彻底阻止 approve-builds）
ignore-scripts=true

# 禁止任何构建脚本（即使 ignore-scripts 未生效，也不会有构建请求）
# 这个设置在 pnpm v10 中有效
only-built-dependencies=[]

# 提升所有依赖到根 node_modules
public-hoist-pattern[]=*

# 跳过对等依赖检查
strict-peer-dependencies=false
