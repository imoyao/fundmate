# Issue 验收清单代码实证复核（增量报告）

- 日期：2026-09-11
- 仓库：imoyao/fundmate（分支 main-v2）
- 复核范围：全部 open issue（114）+ 近 30 天 closed issue（159）；增量对比基线 `2026-08-14.md`
- 方法：逐 issue 读 `body`+`comments`（本次对基线 15 红旗回代码复核状态变化，对 2026-09-10/11 新增的 1400s 集群抽样读正文+评论），逐条待办回到 `backend/app` / `frontend/src` 代码实证；不采信评论里的"已实现"；每条结论附 文件路径:行号 或"未找到"
- 边界：未关闭/重开/删任何 issue，未改 issue，未 git commit/push，未改本报告目录外任何文件

## 先决性说明（影响解读）

1. 自基线（2026-08-14）以来，open 数由 57 增至 114、closed-30d 由 54 增至 159，主要增量来自 2026-09-10/11 集中创建的**数据基建集群 #1396–#1413**（数据抓取治理 / 估值缺口 / 数据源待定类）。这批 issue 普遍带「现状实测 + 缺口 + 备选方案」或明确「验收」清单，属**调研/缺口跟踪类**，无"声称完成却未落地"的硬伤。
2. 基线 15 红旗中，**大量已被本周期开发消化**（导入子系统重建、`nav_per_10k` 收益链路、Turso 接入等）。本报告重点更新这些红旗的状态变化。

## 本周新增 / 变化（相对 2026-08-14 基线）

- 导入子系统重建落地：新增 `backend/app/services/importer/orchestrator_parse.py` / `orchestrator_holdings.py` / `orchestrator_commit.py`（组合为 `ImportOrchestrator`），前端新增导入向导 `frontend/src/views/asset/investment/import/composables/useImportWizard.ts` + `PreviewStep.vue`，并对账工作台 `reconcile-workbench/index.vue`（含 `confirmHoldingImport`）。→ 直接消解基线 R2/R3/R4/R5。
- 货币基金收益链路闭环：新增 `backend/app/services/money_fund_income.py`，读取 `nav_per_10k` 计算每日收益。→ 消解基线 R8。
- Turso 接入：新增 `backend/app/core/db_factory.py` 的 market 域 Turso 接入（dev 回退本地 SQLite）。→ 降级基线 R6。
- 文档交叉引用大幅补全：`docs/spec/tech-debt.md` §14 已登记 #898/#869/#821/#825/#894/#933/#934/#937 等；roadmap.md 反链 #898/#869。→ 消解基线 R1/R7。
- 数据卫生 / job 治理集群 #1402/#1404 已带显式「验收」清单并部分 PR（#1414/#1415），作者主动"暂不关闭"，纪律良好。

## 红旗清单（按严重度排序，状态相对基线更新）

### 🔴 最高

- **无新增最高红旗**。基线 R1（#898 未登记）已消解。

### 🟠 高

- **R2｜#937 导入持仓 position_import：基线"完全未落地" → 现已实质落地（降为中/已解决）**。证据：`backend/app/services/importer/parsers/e_account_holding.py`、`orchestrator_holdings.py`、`ai_recognizer/recognizers/holding_recognizer.py`；前端 `OcrScenario` 新增 `holding_import`（`frontend/src/api/ocr.ts:12`），`reconcile-workbench/index.vue:480` 调 `confirmHoldingImport`。说明：代码层已连通，E2E 未由本次复核实跑（PR #1414/#1415 称全量 pytest 通过）。建议：P1 补一次真实持仓快照导入的端到端冒烟，确认无静默 catch 吞错。
- **R3｜#933 前端统一提交层双轨直写：基线"仍直写 createPosition" → 已消解**。证据：全仓 `frontend/src/views/asset/**` 已无 `createPosition` 直接调用，仅剩 `confirmImport` / `confirmHoldingImport`；`useQuickEntry.ts` 仅做表单 ref 派发，`useImportWizard.ts:1076/1106/1217` 走 `confirmImportApi`。后端 `ImportOrchestrator` 已建。建议：P3 无，保留现状。

### 🟡 中

- **R4｜#934 简记托盘截图导入：基线"未接入" → 已实现**。`frontend/src/components/QuickEntry/TransactionDrawer.vue:70-134` 截图识别锁定 `txn_import` → 候选落 recon-draft → 统一对账工作台确认入库。建议：P2 实跑验证截图→对账工作台链路无错误吞没。
- **R5｜#825 DB 快照导出/导入：基线"核心能力缺失" → 已实现（资产级快照）**。`backend/app/domains/summary/views.py:78-112` 新增 `/summary/snapshots/`（POST `write_asset_snapshot` / GET `get_snapshots`）。说明：为资产快照而非全 DB 级，与基线"降级为 CSV/快照"预期一致。建议：P3 确认前端是否有对应导入入口（若无则登记缺口或标记设计如此）。
- **R6｜#894 上线计划 Turso：基线"代码零落地" → 已降级（部分落地）**。`backend/app/core/db_factory.py:8-10,68,150,162,173-175` 已接入 Turso（market 域）+ Supabase（user 域）。注意：EdgeOne 部署仍仅为架构设想，未落地。建议：P2 将 #894 正文更新为"Turso 已接入 / EdgeOne 待定"，避免"规划未实施"误导。
- **R8｜货币基金每日收益链路：基线"声称可算实际不可算" → 已消解**。`backend/app/services/money_fund_income.py:19` 公式落地，且 `async_backfill.py:43`、`fund_nav_job.py:97` 已回填 `nav_per_10k`。建议：P3 无。

### 仍存在 / 待核

- **R11｜#273 自选基金「行级拖拽排序」：仍缺失**。代码仅实现**表头列拖拽**（`frontend/src/views/asset/watchlist/index.vue:454,920-1008` 的 `Sortable.create(headerRow,…)`，属 #992），无基金行重排拖拽（全仓搜 `dnd|vuedraggable|drag-sort` 0 命中）。#273 若以"已完成"关闭则仍为"声称 closed 实际未做"。建议：P2 人工确认 #273 关闭口径——补实现或在 issue 标注"降级/不实现"。
- **R10｜#824 T 日不回填边界**：**已实现（2026-09-11 复核 + 修复）**。回填止于 T-1，当日（上海时区 `today_shanghai()`）净值/行情不写入；实现见 `backend/app/services/async_backfill.py:_drop_today()`（分支 `fix/824-t-day-backfill`）。对应 #824 待办项已闭环。
- **R12｜roadmap/features 已完成项 issue 反链**：基线列 5 处无反链；本期 `tech-debt.md` 已大幅补全引用，但 `docs/features/*.md` 全篇反链仍弱。建议：P2 抽检 `features/holding-record.md` 等是否补挂。
- **R13｜Portfolio 家庭隔离进度**：基线称 roadmap 未同步；本期未复核。建议：P3 复核 roadmap:33。

### 🟢 低

- **R14/R15**：design/组件反链与 changelog 细节，本期未复核，维持原建议 P3。

## 新集群 #1396–#1413 分类结论

- **调研/缺口跟踪类（无"声称完成"清单，按归档处理）**：#1413（跨渠道关联覆盖 41.1%，已实测降级主流宽基）、#1407（指数估值历史分位无长历史源，待拍板口径）、#1400（可转债增强数据 cookie 待定）、#1397（估值源稳定性技术债）、#1396/#1403（覆盖池 11% / fund_meta 限流炸弹，治理中）。
- **带显式验收清单、部分 PR、正确保持 open**：#1402（job 治理，PR #1414，作者标注"暂不关闭"因第 1 项语义待拍板）、#1404（数据卫生，PR #1415，死列/游离表删除待拍板）。两者均诚实反映"部分完成"，不计入红旗。

## 建议动作清单（汇总，相对基线收敛后）

| 优先级 | 动作 | 关联 |
|---|---|---|
| P1 | #937 holding import 端到端冒烟（确认无静默 catch） | R2 |
| P2 | #934 实跑截图→对账链路验证 | R4 |
| P2 | #894 正文更新为"Turso 已接入 / EdgeOne 待定" | R6 |
| P2 | 人工确认 #273 关闭口径（补实现或标注降级） | R11 |
| ✅ 已闭环 | #824 T 日不回填边界已实现（`async_backfill._drop_today`）+ 规则写入 `architecture.md` §3.3.3 | R10 |
| P2 | 抽检 docs/features/*.md 补 issue 反链 | R12 |
| P3 | #825 前端导入入口确认 / 或标记设计如此 | R5 |
| P3 | roadmap:33 Portfolio 隔离进度复核 | R13 |

## 文档 ↔ 现状差异拉平（铁律 2）

- 基线称"文档声称✅但代码完全缺失"的硬冲突**仍无新增**；且基线 R8（货币基金）/R6（Turso）的"代码缺失"已不成立，应视为文档追上代码。
- 新代码（导入子系统、`money_fund_income`、Turso）已在 `changelog.md`（如 v4.8.6）有部分条目，但**无独立 `docs/features/importer.md` 设计文档**（快速扫描未命中）。建议：P3 补一篇导入子系统特性文档，闭合"代码已改文档未更新"类差异。
- `docs/spec/tech-debt.md` §16 数据卫生/job 治理已与 #1402/#1404 双向反链，符合铁律 1。

## 文档 ↔ issue 交叉引用（铁律 1）

- 正向（文档→issue）：`tech-debt.md` 现反链 #898/#869/#821/#825/#894/#933/#934/#937/#948–#976 等；`roadmap.md` 反链 #898/#869。覆盖良好。
- 反向（issue→文档）：#1402/#1404 正文明确引用 `tech-debt.md §16` / `decisions.md D21` / `data-strategy.md`。双向闭环，无新增缺失。
- 遗留：#273 与 roadmap/watchlist.md 的"拖拽"表述需对齐（见 R11）。

## 临时文件说明

本次复核过程中曾向仓库根写入 4 个 `_issue_*.json` 临时文件用于读取 issue 正文，已通过 `backend/scripts/cleanup_temp.py --apply` 全部删除，仓库恢复干净，未留下任何本报告目录外的文件。
