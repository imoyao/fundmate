# Issue 验收清单代码实证复核（增量报告）

- 日期：2026-09-19
- 仓库：imoyao/fundmate（当前分支 `fix/1610-1611-envelope-spec`，基于 dev）
- 复核范围：全部 open issue（约 130，较基线 114 净增）+ 近 30 天 closed issue（≥2026-08-20，持续高位）；**增量对比基线 `2026-09-11.md`**
- 方法：逐 issue 读 `body`+`comments`（对新增/关闭的关键 issue 全文取证），逐条待办回到 `backend/app` / `frontend/src` / `docs` 代码实证；不采信评论里的「已实现」；每条结论附 文件路径:行号 或「未找到」
- 边界：未关闭/重开/删任何 issue，未改 issue，未 git commit/push，仅写入本报告目录（外加自动化 memory 更新）

## 先决性说明（影响解读）

1. 自基线（2026-09-11）以来，**新增 open issue 25 个**（≥2026-09-11）：#1434/#1435/#1436/#1440/#1449/#1460/#1467/#1490/#1502/#1525/#1535/#1546/#1549/#1554/#1568/#1599/#1602/#1605–#1612。其中 #1605–#1612 为 **2026-09-19 一次性 AI 架构审查产出的 8 张技术债卡**，均有具体行号与实测证据。
2. 同期 **closed issue 大量**（≥2026-09-11 关闭含 #1566/#1557/#1558/#1545/#1547/#1548/#1506/#1519/#1514/#1513/#1520/#1521/#1503/#1499/#1474 等）。本报告对 #1566/#1557/#1506/#1519 做了**代码回证**，均为诚实关闭（见下）。
3. 当前工作树正处于 **#1610/#1611 错误信封修复分支**，多份 `views.py` 处于未提交修改态——这是本期唯一发现的「编辑态缺陷」红旗来源。

## 本周新增 / 变化（相对 2026-09-11 基线）

- **新架构审计簇 #1605–#1612（2026-09-19 创建）**：均带精确行号 + 实测，性质为「已有规范 / 设计 vs 代码不符」或「架构债务」。其中：
  - #1605 跨域 SQL join（导入主链路，双库下抛 `no such table`）
  - #1606 视图层承担业务/事务（ledgers 1811 行 / watchlist 1445 行）
  - #1607 core 反向依赖 domains + domains↔services 双向互引 23 对
  - #1608 测试单例早绑定 / #1609 事务范式两极并存（服务内建 commit vs 视图单事务）
  - #1610 错误响应绕过统一信封（63 处缺 `error_code`，temperature 3 处连 `data` 都缺）
  - #1611 18 端点无尾斜杠 + api.md 滞后（30/131）+ 日志规范例外
  - #1612 导入编排器（orchestrator_holdings/orchestrator_parse，1513 行）无直接测试
- **基线红旗状态变化**：
  - 基线 R11（#273 行级拖拽）：本期确认 **#273 已于 2026-08-12 被「归档保留」**（评论明确「非单一 actionable，收纳至路线图索引 #920，落地时从 #920 reopen」），非「以 completed 关闭」——**R11 消解**（诚实归档，无「声称做了实际没做」）。
  - 新增 #1605 描述的跨域 join 缺陷，经代码回证**已在当前工作树消失**（见 R3）。
- **已核验真实完成（closed，代码确认，非仅评论）**：#1566（V8 守卫）/ #1557（暗色令牌）/ #1506（栅格文档降级）/ #1519（Turso connect_args）/ #1548（1400px 收敛）。见「已回证」段。

## 红旗清单（按严重度排序）

### 🔴 最高
- 无新增最高红旗。

### 🟠 高

- **R1｜`fix/1610-1611-envelope-spec` 工作树疑似引入语法错误（编辑态缺陷 / 生产不可用类）**。
  证据：`backend/app/domains/temperature/views.py:92`（当前未提交修改）
  ```plain
      if not source:
          return jsonify({'message': 'source 参数必填', 'error_code': 1001, 'data': None}    target_date = None
  ```
  信封修复编辑把 `return jsonify(...)` 与下行 `target_date = None` 合并到同一行，且原 `), 400` 状态码丢失。后果：该文件会 `SyntaxError` 导致整个 `temperature` 蓝图导入失败 → **全部温度计端点 500**；即便解析不报错也会把 400 变 200。属「代码存在但生产不可用 / 编辑引入缺陷」。
  处理边界：这是用户**未提交的 WIP 分支**，可恢复；合并前必须 `ruff check` / `python -m py_compile` 全量校验本次 10 个 `views.py` 改动。
  建议：**P1** 合并前对该分支跑 `ruff check .` + `py_compile` 全量；优先修 `temperature/views.py:92`（补回 `), 400` 与换行）。

### 🟡 中

- **R2｜新架构审计技术债 #1605–#1612 未登记入 `docs/spec/tech-debt.md`（铁律 1 正向引用缺失）**。
  证据：在 `tech-debt.md` 全文中检索 `1605|1606|…|1612|1616` 仅命中 1 处（line 641，与本次无关）；8 张技术债卡均未被 `tech-debt.md` 收录。审查报告本身存在于 `docs/working-notes/backend-architecture-audit-2026-09-19.md`（17.98 KB，已引用这些编号），但 `tech-debt.md` 作为 AGENTS.md 指定的权威技术债登记处未同步。
  注意：这是「登记缺口」非「声称完成却未做」；但按铁律 1，文档→issue 应可反查，否则会再次出现基线曾有的「审计发现无人跟进」问题。
  建议：**P2** 在 `tech-debt.md` 新增 § 收录 #1605–#1612（至少 #1605/#1606/#1607/#1610/#1611 五张高信号卡），或显式写明「完整清单见 `backend-architecture-audit-2026-09-19.md`」。

### 🟢 低 / 观察

- **R3｜#1605 跨域 join 缺陷已在代码消失，但 issue 仍 open（小）**。证据：原 `orchestrator_parse.py:351-366` 的 `Fund×Position` / `Fund×WatchlistItem` 跨域 join 在现行 `backend/app/services/importer/**` 中**未找到**；仅 `orchestrator_holdings.py:371` 保留 `PositionImportMeta.join(Position,…)`（user 域内 join，合规）。与 PR #1616 修复一致。issue 评论称「已修复」但未关闭——建议 PR #1616 合并确认后关闭 #1605。非红旗，仅提示收口。
- **观察｜#1611 的 api.md 滞后：owner 已决策「apiflask 自动生成文档，不需手写维护」**（issue 评论 2026-09-19）。故 B 项非阻塞；A 项（18 端点无尾斜杠）仍是真实规范违反（`conventions.md` §2.6 冻结区），修复须前后端同步以免 308→404。
- **观察｜#1549（探市/温度计 Epic）诚实保持 open 且部分完成**：#1552/#1553/#1555 已合并（P0 对比度 / 深度档并发 / 设计一致性），但 T2.2–T2.5（骨架屏 / 超时降级）阻塞于「大类资产 20 卡是否折叠」的产品决策，已在 issue 内显式标注未做。无「声称完成」误导。

## 已回证「真实完成」的 closed issue（抽样，非仅信评论）

| issue | 声称完成 | 代码实证 | 结论 |
|---|---|---|---|
| #1566 | V8 并发构造守卫 + 开关回滚 | `backend/app/core/v8_guard.py` 存在(15KB)；`app/__init__.py:49-51` `install_v8_guard()`；`tests/test_v8_guard.py` 存在；`industry_crowding.py:877`/`fundfof_crowding.py:23-25,147,353` 以 #1566 归因并恢复默认开启 | ✅ 诚实关闭 |
| #1557 | 暗色业务令牌失效 | `frontend/src/style/dark.scss:206` 为 `:root.dark {`（0,2,0）、`:332` 为 `:root.dark[data-theme="showbuy"]`，与评论行号吻合 | ✅ 诚实关闭 |
| #1506 | 12 列栅格「全仓零实现」→ 文档降级 | `docs/design/components.md:85-87` 显式标注「从未实现，短期内不打算实现（#1506 核实）」；#1548 收敛 1400px（line 102） | ✅ 诚实关闭（方案 A 文档降级） |
| #1519 | Turso/libSQL 被塞 SQLite 专有 `timeout` | `db_factory.py:98` `_SQLITE_ONLY_CONNECT_ARGS`、`:111-125` `_sqlite_connect_args` 非本地返回 `{}`、`:371-384` 兜底闸门剔除 SQLite 专有 args | ✅ 诚实关闭 |

→ 本周期**未发现「声称完成实际未做」的硬伤**（closed issue 抽查均真实落地）。

## 建议动作清单（汇总）

| 优先级 | 动作 | 关联 |
|---|---|---|
| P1 | `fix/1610-1611-envelope-spec` 合并前全量 `ruff check`+`py_compile`，修 `temperature/views.py:92` 合并行（补 `), 400` 与换行） | R1 |
| P2 | `tech-debt.md` 收录 #1605–#1612（或显式指向审计 working-note） | R2 |
| P3 | PR #1616 合并确认后关闭 #1605 | R3 |
| 已闭环 | #273 归档（非 completed）→ 基线 R11 消解 | R11 |
| 已闭环 | #1566/#1557/#1506/#1519/#1548 代码回证真实完成 | — |
| 备注 | #1611 B 项（api.md）按 owner 决策不阻塞；A 项尾斜杠修复须前后端同步 | #1611 |

## 文档 ↔ 现状差异拉平（铁律 2）

- **无新增「文档声称已实现但代码缺失」硬冲突**。本期新增的 #1605/#1606/#1607/#1608/#1609/#1610/#1611/#1612 本身就是「规范/设计 vs 代码不符」的**差异登记载体**（如 #1611 指出 `conventions.md` §2.6 冻结区要求尾斜杠但 18 端点违反），差异已被 issue 捕获而非埋在文档里。
- 审计报告 `docs/working-notes/backend-architecture-audit-2026-09-19.md` 已存在并与 issue 互链；与其重复的是 `tech-debt.md` 未收口（见 R2）。
- 前端设计令牌：#1557 修复后暗色令牌首次真正生效，#1558（WCAG 剩余对比度）仍 open 跟进，属正常分批，无文档误述。

## 文档 ↔ issue 交叉引用（铁律 1）

- **正向（文档→issue）**：
  - `docs/spec/decisions.md` 已反向引用 #1566/#1557/#1548/#1506（关闭项留痕良好）✅
  - `docs/working-notes/backend-architecture-audit-2026-09-19.md` 引用 #1605–#1612 ✅
  - **缺口**：`docs/spec/tech-debt.md` 未收录 #1605–#1612（见 R2）⚠️
- **反向（issue→文档）**：#1605/#1606/#1607/#1610/#1611 正文均引用 `architecture.md`/`conventions.md`/`data-strategy.md` 对应条款，且指向审计 working-note；#1549 引用 `frontend/design.md`/`docs/design/*` 与 #1548/#1460。双向基本闭环。
- 遗留：基线 R12（features/*.md 反链弱）本期未新复核，维持原 P2 建议。

## 临时文件说明

本次复核向 `$env:TEMP` 写入 `fm_open.json` / `fm_closed30.json` / `iss_*.json` 共 13 个临时文件用于读取 issue 正文与列表，均位于仓库外，复核结束即清理（不留在仓库、不污染 `docs/working-notes/issue-triage/auto/` 之外任何文件）。
