# Issue 验收清单代码实证复核（增量报告）

- 日期：2026-08-14
- 仓库：imoyao/fundmate（分支 main-v2）
- 复核范围：57 个 open issue + 54 个近 30 天 closed issue
- 性质：首次运行，输出全量基线（同目录无前序报告可比）
- 方法：逐 issue 读 `body`+`comments`（本地 JSON 落盘），逐条待办回到 `backend/app` / `frontend/src` 代码实证；不采信评论里的"已实现"；每条结论附 文件路径:行号 或"未找到"
- 边界：未关闭/重开/删任何 issue，未改 issue，未 git commit/push，未改本报告目录外任何文件

## 先决性数据矛盾（影响解读，必须先行说明）

1. 任务描述中 #824/#825 内容写反，以数据为准：#824 = 按需回填 + T日不回填 + sync 入口职责；#825 = DB 下载导入层（快照导出/导入，full-sync 限范围）。
2. 任务称 #873–#883 为 open，但数据中这批均为 CLOSED，且被路线图索引 #920 明确收纳为"纯产品设想/付费候选，打归档标签关闭，不删除"。故本批次对它们的"逐条落地实证"结果为"未落地"属预期，建议按 #920 归档结论处理，不视为红旗。
3. 本地 issue JSON 为紧凑单行（body 超长），纯文本 Read 不可用，Grep 对超长行中段会截断；"验收清单"以 标题 + 已知范围 + 代码实证 为主轴提取，含 `[ ]/[x]` 清单的 issue 已单独定位。

## 本周新增 / 变化（基线首跑，列当前状态重点）

- 新活跃开发集群 #919–#938（导入/持仓/OCR/统一提交层）均无验收清单闭环，处于"设计/进行中"态，正文多引用彼此作为依赖。
- 审计整改链 #899→#911/#912/#913 已 closed，但其未闭环项的落地需回代码验证（见红旗）。
- 路线图索引 #920 已把 #873–#884 设想类批量归档关闭，这是 open→closed 的主要变化源。

## 红旗清单（按严重度排序）

### 🔴 最高

- **R1｜#898 文档与代码误差追踪会议纪要（open）未在 tech-debt.md 登记**：全库 `docs/**` 搜 `898`/`误差追踪`/`会议纪要` 0 命中，tech-debt.md 第 14 节"issue 残留缺口登记"未纳入 #898。本复核即其落地动作之一。建议：P0 在 tech-debt.md 新增 #898 登记小节回填本结论。

### 🟠 高

- **R2｜#937 导入持仓 position_import 完全未落地**：后端无 `position_snapshot` 解析器（`backend/app/services/importer/parsers/` 仅 alipay_fund/alipay_pdf/standard/ths_stock/tiantian_fund）；前端 `/inventory` 无"导入持仓快照"卡片（`frontend/src/views/asset/inventory/index.vue` grep 0 命中）。仅底层 `process_buy_or_deposit` + import_hash 去重就绪（`position_service.py:235-237,259-261`），无入口触发。建议：P0 登记缺口实现快照解析器+卡片（#936/#933/#928 均依赖）。

- **R3｜#933 前端统一提交层 ImportOrchestrator 未落地（去双轨直写失败）**：`composables/useQuickEntry.ts:40` 的 `handleSubmit` 仍回退到 `BuyForm.vue:812` 的 `await createPosition(body)` 直写；`api/importer.ts` 仅有 `confirmImport`，无 preview/submit 编排层。后端 `process_buy_or_deposit` 整手校验+去重已存在，但前端未收敛。建议：P0/P1 打通前端 orchestrator 编排，消除双轨直写。

- **R4｜#934 资产简记托盘截图导入入口未接入**：`OcrImportModal.vue` 仅服务于自选（`createWatchlistItem`），未挂到资产简记 `BuyForm.vue`（grep OcrImportModal/txn_import 0 命中）。OCR 识别与配额能力已存在（`domains/ocr/views.py:127-202`、`ai_recognizer/recognizers/txn_recognizer.py:98-99`），但简记未复用。建议：P1 挂载并接 txn_import scenario。

- **R5｜#825（DB 快照导出/导入）核心能力缺失**：full-sync 限范围已落地（`sync/orchestrator.py:88` 默认增量），但 DB 级 JSON 快照导出/导入端点不存在（`domains/summary/views.py` 无 export/import，前端仅有 CSV 导出）。建议：P0 评估是否真需 DB 快照或降级为 CSV。

- **R6｜#894 上线计划（Turso 双备份/EdgeOne 部署）代码零落地**：后端 grep `turso/libsql/edgeone/cloudflare` 0 命中，`auth.py:150-187` 仍为单库 SQLite；相关文档仅为架构设想。规划与代码严重脱节。建议：P0 标注 #894 为"规划/未实施"，避免给人已上线印象。

- **R7｜#869 韭圈儿 fetcher 修复未记录进文档**：代码已注册生效（`backend/app/services/thermometer/jobs.py:23,109` 的 `JiucaishuoFetcher`），但全库文档 0 反链，读者无法从文档追溯该修复。建议：P0 在文档登记 #869 修复记录+反链。

- **R8｜货币基金每日收益计算链路未打通**（tech-debt.md:69 自述）：`money_fund_daily_worth.nav_per_10k` 已落库，但无任何代码读取该字段，`calculators.py` 不查、`position_service.py:148-150` 只记流水不建持仓。属"声称可算实际不可算"，上线前必查。建议：P1 闭环收益链路。

### 🟡 中

- **R9｜#936 引导矩阵部分落地**：简记底部"去全面盘点"链接已就位（`BuyForm.vue:436`），但"导入持仓快照"独立卡片与空状态首屏引导缺失（依赖 R2/R3）。建议：P1 补齐双卡片+空状态引导。

- **R10｜#824 T 日不回填无实现**：按需回填已落地（`watchlist_service.py:226-227` → `async_backfill.py`），但代码中无"当日未收盘跳过"边界判断。建议：P2 补 T 日边界。

- **R11｜#273 自选基金拖拽排序未实现**（closed）：`watchlist` 目录无 sortablejs/draggable；仅 SearchHistory 收藏拖拽，非自选列表。文档 roadmap:35/watchlist.md:12 已如实标注"未实现"，故非文档矛盾，但属"声称 closed 实际未做"，建议：P2 要么补实现要么在 issue 标注"降级/不实现"。

- **R12｜roadmap/features 多处"已完成"无 issue 反链**：元数据填充(roadmap:21)、债券利息扣税(roadmap:23)、现金管理识别(roadmap:24)、后端标签统一(roadmap:26)；`docs/features/holding-record.md`、`asset-review.md`、`temperature-archive.md`、`temperature-redesign.md` 全篇 0 反链。建议：P1 补挂 issue 或建跟踪 issue。

- **R13｜Portfolio 已带 family 隔离但 P1-23 进度未同步**：`portfolios/views.py` 含 `family_id` 过滤 + `get_owned_or_404`，但 roadmap:33 仍标 P1-23 家庭隔离"进行中"。建议：P2 更新 roadmap 备注。

### 🟢 低

- **R14｜设计体系/通用组件进度缺总反链**（design 体系封箱、MoneyDisplay/RiseFallText 100%）；changelog.md 未单列导入事务一致性修复、温度 B3 文案归集、FeeRatio 币种等条目。建议：P3 统一反链 + 补 changelog。

- **R15｜#864（SidebarLogo/NavHorizontal brand-sub 与 Beta 挂件重建）**：前端 `components/` 未确认 Beta 挂件重建，需人工目视确认（子代理未完全定位，标记为待人工核查，非代码硬证）。

## 建议动作清单（汇总）

| 优先级 | 动作 | 关联红旗 |
|---|---|---|
| P0 | tech-debt.md 新增 #898 登记小节，回填本复核结论 | R1 |
| P0 | 文档登记 #869 fetcher 修复 + 反链 | R7 |
| P0 | 实现 #937 position_import 快照解析器 + /inventory 卡片 | R2 |
| P0 | 打通 #933 前端 ImportOrchestrator 编排，消除双轨直写 | R3 |
| P0 | 评估 #825 DB 快照导出/导入（降级或实现） | R5 |
| P0 | #894 标注"规划/未实施"，避免已上线误读 | R6 |
| P1 | #934 资产简记挂 OcrImportModal 接 txn_import | R4 |
| P1 | 闭环货币基金每日收益链路（读 nav_per_10k） | R8 |
| P1 | 补齐 #936 引导矩阵双卡片+空状态引导 | R9 |
| P1 | roadmap/features 已完成项补 issue 反链 | R12 |
| P2 | #824 补 T 日不回填边界 | R10 |
| P2 | #273 补拖拽实现或在 issue 标注降级 | R11 |
| P2 | 同步 P1-23 Portfolio 隔离进度 | R13 |
| P3 | design/组件反链统一 + changelog 补录 | R14,R15 |

## 文档↔现状差异拉平（铁律2 结论）

- 文档声称"✅已完成"且代码已落地的关键项经核实 12 项均成立（XIRR、Portfolio CRUD、BIAS、JWT 鉴权、交割单解析器、导入事务一致性、Ledger 扩展、交易日 API、后端标签统一、个人中心、FeeRatio 币种、温度 B3 文案）。**无"文档声称✅但代码完全缺失"的硬冲突**。
- 唯一真实缺口为 R8 货币基金收益链路（文档 tech-debt.md:69 已自登记，非文档↔代码矛盾，但确属"声称可算实际不可算"）。
- 代码已改/已实现但文档未更新：R7（#869 修复未记）、R13（Portfolio 隔离未同步）、changelog 若干修复未单列。

## 临时文件说明

本次复核产生的 `.tmp_open_issues.json`、`.tmp_closed_issues.json`、`.tmp_open_full.json`、`.tmp_closed_full.json`、`.tmp_closed_dates.txt` 为临时调试产物，按仓库约定应清理（不在本报告目录外保留业务逻辑文件）。清理动作需人工确认后执行 `backend/scripts/cleanup_temp.py`。
