# Gitee 参考项目（ai_black_ma/ai）实证审计（2026-09-26）

> 调研对象：`D:\codes\ai`（Gitee `ai_black_ma/ai` 完整克隆，本地 44 commits，落后远端 2 个）。
> 方法：3 路代码探查（framework / tests+examples+config+app / deploy+gitops+git 历史）+ 本地 git 历史核验，关键结论均带 file:line 证据。
> 用途：回答「这个参考项目含金量如何」，并固化**可借鉴 / 红线**两份清单。本文件不产出任何会被抄进简历的数字。

## 一、结论先行

一句话定级：**骨架真、皮肉虚、测试是装饰，文档跑在代码前面。**

| 层 | 真实度 | 判定 |
|---|---|---|
| 基础设施（Jenkins / ArgoCD / Grafana / k3s 配置） | ★★★★ 真跑过 | 流水线有 `jenkins-ci` bot 提交实录 + 真实排障提交；但单机演练级（`registry.local`、`root/123456`、应用端口 8000 vs 8002 不一致） |
| 对话 / 上下文工程（`conversation/`、`platform/`、`config.py`） | ★★★★ 值得学 | token 预算分层裁剪 + LLM 摘要兜底真实可用，是全仓最扎实的一块 |
| Web 应用（`app/`） | ★★★ 中规中矩 | 12 个真实路由 + 真 JWT/bcrypt；但插件端点无鉴权、chat 端点鉴权可选（等于没防） |
| ReAct 引擎（`framework/`） | ★★ 装饰性 | while 循环真有，但状态转移表是死代码、registry 契约不匹配大概率每次调用必炸、trajectory 恒为 `[]`、零测试 |
| MCP | ★★ 半成品 | server 真挂载；client 有阻塞 `start()` bug、无 stdio、零调用点 |
| 工具系统 | ★ 不统一 | 3 套互不兼容的工具体系（31 / 13 / 9 个），5 个 web 工具里 4 个是 mock |
| 测试 | ★ 装饰 | 15 文件 98 用例，**0 个 skip 标记**；实测约 17-18 必失败、8-13 缺夹具报错、12-15 假绿 |
| 编排 / 调度 | ★ 占位 | `while: sleep(0.1)` 占位；API key 轮询写了但从未接线 |
| 「16 分片 MySQL」 | ✘ 号码牌 | 实为 16 张表同库、16 个 engine 指向同一实例 |

## 二、三个最有信息量的证据

1. **测试门禁自降级**：`Jenkinsfile:87-97` 让 pytest 失败不阻塞（report 级门禁）——这解释了 98 个用例里约 1/3 是坏的却无人处理：**CI 绿 ≠ 测试绿**。
2. **git 历史三段式**：2026-03-15 一天 29 提交 → 半年空窗 → 09-19 巨型「重构代码」提交 → 09-25/26 两天 40 提交冲刺（含 4 个 `trae-agent` AI 提交）。既有整块灌入（首次上传 60 文件、单提交 >229KB）也有真实排障提交——**这是「为展示而构建」的时间线**，面试被追问「你的活跃期」会露馅。
3. **密钥泄进公开仓**：`config/secrets.yaml:15`、`tests/test_deepseek_direct.py:7` 两把真实 API key 明文入库。（他仓问题，我们只记录提示，不代为处置。）

补充：`docs/07-面试准备` 宣称的参考文件有对不上代码的（文档跑在代码前面）——引用它的题库时以问题为素材，不以它的事实陈述为素材。

## 三、可借鉴（3 条，借「概念」不借「数字」）

1. **对话上下文的预算制管理**（`conversation/`）：按 token 预算分层裁剪 + 关键信息优先召回 + LLM 摘要兜底——S2 记忆层的现成概念框架。
2. **题库的分文件组织**（`docs/07-面试准备`）：概念问答 / 线上案例 / 话术分层存放——我们的题库地图已按 S1~S6 映射（见 `agent-interview-prep-plan-2026-09-25.md` §4）。
3. **工程卫生台账**：ruff `noqa` 逐条注原因的写法，即「例外必须留痕」的纪律，可搬到我们的 lint 基线。

## 四、红线（不可抄）

- 一切规模数字（10 万 DAU、TPS、分片数）——与「无编造数字」纪律冲突；
- Celery / Redis / MySQL 分片 / One API 等它有我们没有的选型——抄了就与我们的「反过度设计」立场自相矛盾；
- 它的面试话术原文——属他人叙事，且经不起追问；
- mock 工具、假绿测试、非阻塞门禁这类「装饰工程」做法。

## 五、含金量分层回答

- **作为学习样本：中上**——infra 层与上下文工程层值得读；ReAct 层当反面教材（有 while 循环 ≠ 有 Agent 引擎）。
- **作为作品集 / 面试样本：下**——测试假绿、门禁自降级、数字注水、活跃期断层，任一条被深挖都失分。
- **对我们的价值**：它的败笔比亮点值钱——S4（评估与验收口径）与「CI 红绿语义」的反面案例直接引以为戒。
