---
title: AI Review 历史误报案例库（不注入 prompt）
---

# AI Review 历史误报案例库

> **本文件刻意不注入 prompt。** 它是维护者归档、复盘与「该加什么规则」的证据台账；
> 把它塞进 prompt 既烧 token 又会锚定模型（2026-09-11 之前它确实在 prompt 里，实测收效甚微，故迁出）。
>
> **追加规则前先来这里登记案例**，流程见 `docs/configs/ai-review-prompt.md` 文末「附」。
> 判断次序：**能降级为配置的，不进 prompt**（审查范围、配额、忽略路径、agent 开关）→ 剩下的才抽象成一般规则。
>
> ⚠️ **口径更新（2026-09-11）**：下表「现在由什么拦住」一列中凡写有「prompt §0 验证义务 / 先看文件 / 必须 `rg` 出证据」
> 者，其原始含义是「模型先读仓库再发言」。但 `agent.enabled` 实测失败已关闭（`.ai-review-deep.yaml`，
> 证据链见 `docs/spec/ai-review.md` §7），**该能力不存在**。`prompt §0` 已同步改写为
> 「**不得对看不见的仓库做任何断言**」。
> 即：拦截方向不变（都是「不许猜」），但从「要求核实」改为「禁止越界断言」——**不再依赖读仓库能力**。

## 一、按类型归档

| 误报类型 | 首次实证 | 模型当时的典型说法 | 事实 | 现在由什么拦住 |
|---|---|---|---|---|
| **符号「未定义 / 未使用 / 重复导入」** | #1344 | 「`TYPE_LABELS` 未定义」「`_user_sort_metric` 找不到定义」「`holding_cost_price` 来源不明」 | 全部已定义：来自 `app.core.constants`、同文件其它函数，或 enrich 阶段下发 | `prompt §0` 的硬禁止：无工具读仓库 → 该类别**一律不得提**。（当时曾寄望 `agent.enabled: true` 提供读权，2026-09-11 实测该配置必致零产出，已关闭） |
| **同一符号同时被判「未使用」与「未定义」** | #1399 | 对 `index.vue` 的 `activeCategory` 同时发「定义未使用」（511/513/515）与「使用未定义」（517） | 511 定义、517 使用，两处都在同一文件 | 同上；两条互斥断言同时出现即自证误报 |
| **同一元素同时被判「被删」与「被加回」** | #1399 | 对 `WatchlistFilterBar.vue` 的 `reverse_repo` 同时发「被删除」与「被加回」 | 从未删除过 | `.ai-review-deep.yaml` 的 `review.mode: ONLY_ADDED_WITH_CONTEXT`（不再把删除行当审查对象） |
| **等价替换被读成删除 / 回归** | #1369（**46 条行内意见全误报**） | 「`TradeService` 的 import 被删，需加回」「同一模块 import 了两次」 | 补丁里 `-` 旧路径与相邻 `+` 新路径是**换源**（`services/trade_rules` → `services/trading`） | 同上（`ONLY_ADDED_WITH_CONTEXT`）+ prompt §2 范围纪律 |
| **建议「已经就是现状」的写法** | #1399 | 建议给 `useWatchlistColumnVisibility` 的 `activeCategory` 加 `?`（可空） | 现状已是 `Ref<string \| null> \| ComputedRef<string \| null>`；照做反而丢掉 `ComputedRef`、收窄类型 | prompt §0「给 suggestion 前必须打开文件核对」 |
| **append-only 历史行被当本次改动** | #1401（**9 条**） | 对 `decisions.md` 的既有决策行提「可能导致破坏」 | 该 PR 对 `decisions.md` 只**追加 1 行**，被点评的行全是历史快照 | prompt §2 范围纪律（append-only 既有行一律不点评） |
| **Markdown 正文被当「代码注释」要求删除 + 语气审查** | #1401（**15 条**） | 要求删除文档正文/引用块、审查措辞语气、把正文包进 `json` 块当 suggestion | 文档 PR 的正文就是交付物 | prompt §7 文档类 PR 审法 |
| **「文档没写清楚」类元建议（要求展开论述）** | #1401（8 条，集中在 `conventions.md` §16.2） | 要求把「数据准入四问」逐条展开成小节 | 与仓库「简洁优先、禁止臃肿」（§16.2 本身）方向相反，且其中 2 条声称缺失的内容原文已有 | prompt §7 末段（「建议补充解释」不属于缺陷） |
| **表格行「未描述内容」** | #1401（1 条） | 称 `index.md` 的表格行未描述职责 | 该表本来就有「职责」列 | prompt §0（断言前先看文件）+ §7 |
| **主动有害的「修正」** | #1401（3 条） | ① 把「被读成『不写多余代码』而不是『不存多余数据』」**改成反过来的**；② 把「新增约 1.2 万行」改成「约 1.2 万行**代码**」；③ 要求在「1.22 GB」后补「（单位：GB）」 | ① 直接反转语义；② 那说的是数据库行数；③ 是数值不是代码 | prompt §1「禁止逆 PR 意图」+ §3「拿不准不提」 |
| **注释 / docstring / 文档的增删改被当缺陷** | #1383（29 条几乎全为此类） | 「应保留原注释」「注释丢失」 | 注释与文档的精简本身不是缺陷 | prompt §6「注释删除不是缺陷」 |
| **约定类反向建议** | #1395（40 条几乎全误报） | 「中文注释应改英文」「mixin 继承顺序应调整」「测试 import 风格应改」「`# added` 注释应删除」 | 均为本仓既定做法 | prompt §6 既定约定清单 |
| **历史记录被读成待办** | #1395 | 把 `decisions.md` 里"当时背景"的叙述读成"当前待办"并建议去修 | 那是 append-only 决策日志 | prompt §6 + §2 |
| **新 Job「缺数据源配置 / `NullAdapter` 未引入」** | #1395 | 「新 job 没有数据源配置」「未看到 `NullAdapter` 的引入」 | 该适配器在 `sync/orchestrator.py` 顶部已 import，经 `_register_jobs()` 注册即接线完成 | prompt §5 后端清单末条 |
| **前端改动「未确认后端支持」** | #1344 | 仅凭前端片段断言后端不支持，建议「先确保后端支持」或删掉属性 | checkout 里有后端代码可直接核对 | prompt §0 验证义务 |
| **单测「未用 db 夹具 / 缺逻辑 / 断言不全」** | #1344 | 「测试函数未使用夹具提供的数据库」 | 纯函数单测不需要 db 夹具 | prompt §0 + §3「建议补测试」的限制 |
| **只读端点「越权（IDOR）」** | #1353 | 「传入 `ledger_id` 未校验归属，可越权读他人数据」 | service 层已按 `family_id` 过滤 | prompt §0（必须先读 service 层再定级） |
| **`PureHttp` 第二泛型被按 axios 原生语义判错** | #1353 | 「第二泛型是响应类型，会导致编译失败」 | 自有封装：`P` 是 `AxiosRequestConfig<P>` 的 data 类型 | prompt §5 前端清单 |
| **`NotRequired` / PEP 655 被判需 3.11+** | #1353 | 「建议改用 `total=False` + `Required`」 | 本仓 `requires-python = ">=3.12"`，原生可用 | prompt §5 通用末条 |
| **字体文件体积、`font-feature-settings` 冗余** | — | 未实测就断言字体「过大」；判定两个 CSS 属性冗余 | 语义不同，且体积未测量 | prompt §3（无证据不提） |
| **`server_default` / `nullable` / PDM lock 版本变化被判风险** | — | 「SQL 表达式问题」「lock 降级」 | 常规演进 / 工具重生成的正常产物 | prompt §6 |
| **把「中文优先」反向读成「注释必须用英文」** | #1416（**5 条全为此类**） | 「代码注释中使用了中文，**违反了中文优先的约定**」，并建议「将注释中的中文翻译成英文」 | 自相矛盾：`中文优先` 约束的是 **AI 的回复语言**，而 prompt §6 第 129 行已白纸黑字写明「注释/docstring 普遍用中文，是既定约定；不要以『应保持英文风格』为由提意见」 | **无**（见第四节：prompt 已写明仍违反 → 属模型档位问题，非措辞问题） |

## 二、量化证据（可复核）

| PR | 类型 | 行内 + 整体意见数 | 经核实为真缺陷数 | 备注 |
|---|---|---|---|---|
| #1369 | 重构（模块合并 / 换源） | 46 | 0 | 全为「import 换源被读成删除」一类 |
| #1383 | 注释 / 文档清理 | 29 | 0 | 几乎全为「应保留原注释」 |
| #1395 | 数据 job 新增 | 40 | 0 | 约定类反向建议为主 |
| #1399 | 前端（自选表列） | 38 | 0 | 含两条自相矛盾断言 |
| #1401 | 纯文档（数据策略） | 52 | 0（真缺陷 4 处，**全由人工自查发现**） | 见下节 |
| #1412 | AI review 配置改造本身 | 5 + 1 | 0 | **降噪后的首轮实测**：数量已收敛到硬配额（inline ≤ 5） |
| #1414 | 后端同步 job 契约 | 0 | — | 上游 429，审查未真正执行（被守卫判失败） |
| #1416 | 前端 dev server 插件 | 5 + 1 | 0 | 5 条**全是同一类**「中文注释违反中文优先」 |

## 三、一个反直觉但重要的教训

PR #1401 的 4 处**真**缺陷（决策编号漏标导致三处交叉引用悬空、两个文档对同一事实口径矛盾、引用措辞指错行、同一句混用两种引用风格）**AI 一条都没报**，全部由人工自查发现。

也就是说：这套 bot 当时既在**产出大量误报**，又在**漏掉真缺陷**——两头不讨好。原因不是「模型不够强」，而是**接口给错了**：

1. `review.mode = FULL_FILE_DIFF` → 送去的是整个文件，模型对没改过的行也评论、也把删除行当现况；
2. `agent.enabled = false` → 模型读不到仓库，只能凭补丁文本猜，「未定义 / 未使用」类断言必然大面积出错；
3. 无配额、inline 失败降级为普通评论 → 单 PR 可产出几十条；
4. 应对方式一直是「往 prompt 里追加禁止事项」→ 16 天从 8.7KB 涨到 29KB，且**已被禁止过的类别继续复发**。

→ 结论：降噪要改**接口与结构**，不是把禁止清单写得更长。2026-09-11 的改造即按此执行（见 `.ai-review-deep.yaml` 头部注释与 `docs/spec/tech-debt.md`）。

## 四、结构改完之后剩下的瓶颈：模型档位（2026-09-11 实证）

结构调整把**数量**压下去了，但**质量**没有同步改善。PR #1416 提供了一个干净的反证：

- 该 PR 只新增一个 92 行的 vite 插件，逻辑简单、类型检查与端到端冒烟全通过；
- AI 产出 **5 条 inline + 1 条 summary**，5 条 inline **全部是同一类**：称「代码注释中使用了中文，违反了中文优先的约定」；
- 而 `docs/configs/ai-review-prompt.md` 第 129 行**早已明确写着**：「注释/docstring 普遍用中文，是既定约定；不要以『应保持英文风格』为由提意见」。

即：模型不是「没看到规则」，而是**看到了规则、抓住了「中文优先」四个字、然后反向套用**。同一轮日志显示当次 `CHOSEN: glm-4-flash (selected_ok=true)` ——**智谱免费档**，付费的火山方舟 DeepSeek 因额度用尽全程 429 未参与。

**可操作的结论**：

1. 在当前免费档模型下，**继续加 prompt 措辞无效**——规则已写明仍被违反；
2. 质的改善**只能靠换回付费档模型**（恢复 `ARK_API_KEY_CODEREVIEW` 额度，或在 `scripts/llm_health_probe.py` 的候选清单里换一家）；
3. 额度未恢复前，deep-review 的产出**不应作为合并依据**（它是非必需检查，不阻塞合并，这是有意为之）。

判断依据可直接复核：`.github/workflows/ai-review.yml` 的探测步骤会打印 `CHOSEN: <model>` 与各候选的 `429 限流/额度用尽` 状态。
