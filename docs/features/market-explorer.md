---
title: 探市（大类资产观察）设计
---

# 探市 · 大类资产观察 设计

**状态**: 设计稿（待评审，未实现）
**日期**: 2026-09-11
**依据**: ① 府库《做了一个星期，观察大类资产真不容易》（20 资产组合 + 三个数据坑）；② 「全A等权 500 日线温度计」截图；③ **本仓代码与数据源实测**——本文第 2 节全部为 2026-09-11 真接口实测结果，非推断

**相关**: 温度页**视觉/交互**方案见 [`temperature-redesign.md`](./temperature-redesign.md)（本文只补其数据侧）；数据分层口径见 [`../spec/data-strategy.md`](../spec/data-strategy.md)；刷新现状台账见 [`../spec/data-refresh-inventory.md`](../spec/data-refresh-inventory.md)

## 0.1 2026-09-12 修订：本设计是**改造既有页**，不是新建页面

**这是一处方向性纠正。** 初稿把探市页当成待建的新页面，实测发现**它早已存在**：

| 已有资产 | 位置 | 内容 |
|---------|------|------|
| **探市页** | `frontend/src/views/explore/` | `MarketHeader` → `ExploreTemperatureDashboard`（温度仪表盘）→ 转化区 → `ExploreAddSection`（仅未登录）→ `ExploreWatchlistTable`（观察列表）→ `PageFooter` |
| **温度计页** | `frontend/src/views/temperature/` | `index.vue` + `BiasTable` / `CrowdingTable` / `MetricDetailTable` |
| 共用组件 | `components/MarketHeader` / `MarketFooter` / `PageFooter` / `BrandLogo` | 探市与温度计**强制同一套**品牌头尾 |
| 温度卡 | `components/TemperatureGaugeCard` | `size: 'lg'` 温度计英雄卡 / `'sm'` **探市页入口卡** |
| 追踪 issue | `#808`（探市体验版设计）· `#821`（探市页缺陷与缺口）· `#893`（探市 × 自选 重新评估纪要）· `#869`（韭圈儿 fetcher 未注册，已闭环） | **设计前必须先读这几张，不要重复发明** |

因此本文的正确读法是：

- **§2~§5（数据源 / 口径 / 分层 / 算法）= 全新增量**，与既有页无关，照旧有效；
- **§6（界面）= 修改为「在既有探市页内新增一个「大类资产观察」区块」**，而非独立页面；
- 该区块**必须复用** `MarketHeader` / `PageFooter` / `TemperatureGaugeCard` 等既有组件与设计语言，
  并落在 `views/explore/components/` 下（与 `ExploreTemperatureDashboard` 同级）；
- 现有温度仪表盘已用 `batchFetchQuotes`（沪深300 / 中证500 / 创业板指）与 `fetchTemperature` 取数，
  **新增大类资产区块需要新的后端端点**（当前 `src/api/` 下无 market/overview 类接口）。

### 0.1.1 四项决策已定（2026-09-12）

| # | 决策 | 依据 |
|---|------|------|
| 1 | **海外 4 资产缺源不阻塞本期**：先做有数据的，缺的**置灰 + 文字提醒 + 软占位** | 用户决策；缺口跟踪见 `#1436` |
| 2 | **商品优先用国内口径** | 实测：国内（上期所/INE 主力连续）与海外（GLD/SLV/USO）的**当日涨跌幅符号相反率** 黄金 **67%**、白银 **75%**、原油 **42%**（近 12 个重叠交易日）——差异是**结论级**的，不是数值级。用户条件「区别大就用国内」成立，但**必须标注口径（含夜盘）**，详见 §3.2 |
| 3 | **债券价格与收益率都显示**（双轨） | 两者反向，**同时显示本身就是信号**（原文「股债一起涨或一起跌就值得留意」的正解）。见 §3.4 |
| 4 | **数据底座（每日定时任务）后置**，先做探市新功能 | 用户决策；底座问题跟踪见 `#1434`（调度失败）与 `#1431`（东财通道不可达） |

> 另：**市场宽度（`% 处于 200/500 日均线以上`）暂无专卡**。最接近的是
> **`#1407`【指数估值「历史分位」缺口：可用数据源都不提供长历史，需定口径】**，
> 但两者**不是同一件事**——`#1407` 是「指数估值分位」，市场宽度是「全市场个股在均线上的占比」。
> 调研时须把这条差异一并判清。

## 0. 一句话

- **探市页** = 20 个大类资产的「当日涨跌 + 相对位置」一览，解决「我现在该看哪儿」；
- **温度计** = 把「市场贵不贵」压成一个 0~10 ℃ 的数，解决「现在能不能买」。

两者共用**同一套底座**：指数日线 → 均线偏离 / 历史分位。**不引入新依赖。**

## 1. 现状盘点：能直接复用什么

| 已有资产 | 位置 | 对本设计的价值 |
|---------|------|--------------|
| 7 个温度源 fetcher | `services/thermometer/fetchers.py` | 东财成交额 / 集思录转债 / 且慢 / 有知有行 / 韭圈儿 / 集思录指标 / **自算分位**（`SelfCalcFetcher`）——「自算」这条链路就是温度计的现成范式 |
| `index_daily`（指数日线） | `domains/indices` | **已落 10 个万得系指数**（单指数 2,104 ~ 6,466 行、最长 ≈26 年），尾部到 2026-09-08 ⇒ **算 500 日均线绰绰有余** |
| `index_valuations`（中证官方 PE / 股息率） | `domains/indices` | 模型与 job 已建；**但真库实测 0 行**（从未回填成功） |
| `market_single_values` / `market_composites` / `market_multi_items` | `domains/temperature` | 温度/合成/多品种三张表齐全，前端已有 `views/temperature/` 三个表组件（乖离率 / 拥挤度 / 指标明细） |
| `app/tools/scheduler.py` | 每日调度入口 | 已具备「外部定时器 + jitter」的调度壳（#1182），20:00 发布可直接挂上 |
| 依赖 | `pyproject.toml` | 只有 `akshare` + `baostock`——**本设计的硬约束是「不新增依赖」** |

> **必须点出的现状**：温度模块 6 个源在真库里**全部停在 2026-08-02**（各 9 行），
> `index_valuations` **0 行**。也就是说这套东西**代码齐全、数据断供一个多月**——
> 与净值停在 08-14 是同一类问题（见 `data-refresh-inventory.md` §5）。
> **本设计不是从零造页，而是先让已有底座重新跑起来。**

## 2. 数据源实测（2026-09-11 · akshare 1.18.91）

### 2.1 通道健康度 —— 本设计最重要的约束

| 通道 | 状态 | 实测证据 | 影响 |
|------|------|---------|------|
| **新浪** | ✅ **全通** | `index_us_stock_sina` 1.0~2.0 s、`stock_hk_index_daily_sina` 1.1~1.6 s、`currency_boc_sina` 2.7 s、`futures_main_sina` 1.4 s、`stock_us_daily` 3.4 s | **本设计的主通道** |
| **东财 `push2` / `push2his`** | ❌ **全挂** | `index_global_spot_em` / `index_global_hist_em` / `stock_hk_index_*_em` / `forex_hist_em` / `futures_global_hist_em` **全部** `Max retries exceeded` 或 `RemoteDisconnected` | 凡依赖东财的资产**必须换源** |
| 中债 / 官方 | ✅ 通 | `bond_zh_us_rate` 4.3 s / 717 行 | 债券收益率 |
| 韭圈儿 | ✅ 通 | `index_daily` 尾部 2026-09-08 | 长历史指数 |

> 东财通道整体不可用与 `docs/bias-datasource-troubleshooting.md` 记录的历史一致（限流 / 封 IP，本机叠加 DevSidecar）。
> **设计含义**：任何「按东财接口设计」的方案会在本机 100% 跑不通——这与文章里作者「一周换 4 种取数方式」是同一个问题。

### 2.2 20 个资产的源映射

实测列 = 本次真接口跑过；推定列 = 同通道同接口、未逐一实跑（实现前须补测）。

| # | 类别 | 资产 | 接口 | 实测结果 | 状态 |
|---|------|------|------|---------|------|
| 1 | A股 | 上证指数 | `stock_zh_index_daily(sh000001)` | ✅ 8,724 行 | 可用 |
| 2 | A股 | 沪深300 | `stock_zh_index_daily(sh000300)` | ✅ 5,991 行 | 可用 |
| 3 | A股 | 中证500 | `stock_zh_index_daily(sh000905)` | 同通道（推定） | 待补测 |
| 4 | A股 | 中证2000 | `stock_zh_index_daily(sh932000)` | — | ⚠️ **文章明确说交易所不披露、取不到**，需另找或降级 |
| 5 | 港股 | 恒生指数 | `stock_hk_index_daily_sina('HSI')` | ✅ 3,215 行 | 可用（东财版已挂） |
| 6 | 港股 | 恒生科技 | `stock_hk_index_daily_sina('HSTECH')` | ✅ 1,492 行 | 可用（**文章作者此处「拿不到」，我们靠新浪通道取到了**——选源差异，非能力差异） |
| 7 | 海外 | 标普500 | `index_us_stock_sina('.INX')` | ✅ 5,711 行 | 可用 |
| 8 | 海外 | 纳斯达克 | `index_us_stock_sina('.NDX' / '.IXIC')` | ✅ 3,159 / 5,708 行 | 可用，**需定口径**（纳指100 还是综合） |
| 9 | 海外 | 日经225 | `index_us_stock_sina('.N225')` | ❌ `list index out of range` | ⚠️ **缺源** |
| 10 | 海外 | 富时100 | `index_us_stock_sina('.FTSE')` | ❌ 同上 | ⚠️ **缺源** |
| 11 | 海外 | 德国DAX | `index_us_stock_sina('.GDAXI')` | ❌ 同上 | ⚠️ **缺源** |
| 12 | 海外 | 法国CAC40 | —（同属欧股，新浪不覆盖） | 未测（推定缺源） | ⚠️ **缺源** |
| 13 | 债券 | 国债ETF（**价格轨**） | `fund_etf_hist_sina('sh511010')` | ✅ **3,266 行** | 可用（**本轮补齐**；东财版 `fund_etf_hist_em` 已挂，新浪通道通） |
| 14 | 债券 | 美债ETF（**价格轨**） | `stock_us_daily('TLT')` | ✅ **2,668 行**（尾到 2026-09-11） | 可用（**本轮补齐**，与 GLD 同美股通道） |
| 15 | 商品 | 黄金 | `futures_main_sina('AU0')` 沪金主力（**国内 · 已定**） | ✅ 4,551 行；海外 `stock_us_daily('GLD')` ✅ 5,486 行 | 国内为主，海外作对照（§3.2） |
| 16 | 商品 | 白银 | `futures_main_sina('AG0')` 沪银主力（**国内 · 已定**） | ✅ 3,492 行；海外 `SLV` ✅ 5,124 行 | 同上 |
| 17 | 商品 | 原油 | `futures_main_sina('SC0')` INE 原油主力（**国内 · 已定**） | ✅ 2,056 行；海外 `USO` ✅ 5,137 行 | 同上 |
| 18 | 商品 | 比特币 | `crypto_js_spot()` | ✅ 10 币种**实时快照**；另测 `crypto_bitcoin_cme`（5 行成交/持仓）、`crypto_bitcoin_hold_report`（59 行持币公司） | ❌ **历史确证不可得**（akshare 内无 BTC 价格历史接口）：只能展示当日涨跌，做不了分位 |
| 19 | 汇率 | 美元指数 | `currency_boc_sina('美元')` | ✅ 803 行 | ⚠️ 是中行**牌价**，非 DXY 指数 |
| 20 | 汇率 | USDCNH（离岸） | 穷举：`forex_hist_em` ❌东财挂 / `fx_spot_quote`、`fx_pair_quote`（25 + 16 对**无 CNH**）/ `fx_quote_baidu` ❌风控 / `currency_pair_map` ❌超时 / `currency_*` **需 api_key** | ❌ 全部不可用 | ❌ **不可得**；唯一替代是 `currency_boc_sina('美元')` 的**在岸**中行牌价/央行中间价（口径不同，须标注） |

**结论：不必引入 Tushare / yfinance / TickDB。** 20 项的最新盘点（**2026-09-12 二轮实测后**）：

| 档位 | 项数 | 资产 |
|------|------|------|
| ✅ 源已实测直通 | **11** | 上证 / 沪深300 / 恒生 / 恒生科技 / 标普500 / 纳斯达克 / 国债ETF / 美债ETF / 黄金 / 白银 / 原油 |
| ◻️ 同通道待补测 | **1** | 中证500（`stock_zh_index_daily(sh000905)`） |
| ⚠️ 只能降级 / 换口径 | **2** | 中证2000（交易所不披露）、美元指数（只有中行牌价，非 DXY） |
| ❌ 确证不可得 | **6** | 日经225 / 富时100 / 德国DAX / 法国CAC40 / 比特币**历史** / 离岸人民币 USDCNH |

与初稿相比，**净变化是「债券从缺口转为已解决」**——本轮实测补齐了 ETF 价格双轨的国内 + 海外两个源；
商品口径（决策 2）与汇率口径也随之收敛。**真正的净缺口是 6 个资产位，成因单一**：
全是「本机网络出口取不到的境外源」，与是否新增依赖无关（`yfinance` 已实测无效，见下）。

尤其值得说明：文章里作者引入 Tushare、yfinance、TickDB、Trading Economics **四套**外部源来拼，
而我们的同源替代是「**一个 akshare + 通道优选**」——代价是接受这 6 项缺口，
收益是不新增依赖、不新增密钥、不新增合规面。**建议先把 11 项直通 + 1 项补测做出来，缺口单独挂 issue。**

#### 2026-09-12 复核实测：海外缺口的常见替代方案**全部不通**

（详见 `#1436` 的实测评论）

| 候选方案 | 实测结果 |
|---------|---------|
| `yfinance`（Yahoo） | ✅ 已按「加依赖」路线**在隔离环境实测**（`D:/codes/_venvs/yf-probe`，yfinance **1.7.0**）：**装了也没用** —— `yf.download` 与 `yf.Ticker().history()` 在**含代理与清除代理直连两条路径**下，对全部 6 个符号均返回 `YFRateLimitError: Too Many Requests`；裸 `requests` 直连 Yahoo chart API 返回 **HTTP 403 + 中文拦截页**（`<html lang="zh">`，3369 字节 HTML，非 JSON）⇒ **Yahoo 对本机整体拒绝（地区级封锁）**，不是缺依赖 |
| `stooq` CSV | **已加 JS 反爬** —— 响应体为「This site requires JavaScript to verify your browser」；正确符号（`^nkx`/`^ukx`/`^dax`/`^cac`）与对照组（`^spx`/`aapl.us`）一并失败 |
| akshare `index_investing_global`（英为财情） | **1.18.91 中不存在**（`exists=False`） |
| akshare `index_global_hist_em` / `index_global_spot_em` | 存在，但**都是东财通道**（已挂，见 `#1431`） |

⇒ **在现有依赖 + yfinance 均不可用的情况下，海外 4 资产无解。** 因此「软占位」不是临时凑数，
而是**当前唯一零成本答案**——它的地位已从「待定」升级为「**结论**」：
除非换到能访问 Yahoo / stooq 的网络出口，否则不必再在这 4 个资产上投入。
#### 2026-09-12 二轮实测：债券源补齐，CNH 与比特币历史**穷举确认不可得**

按「把推定项实跑一遍」的原则，本轮把 §2.2 里所有「推定 / 待补测 / 待找」的候选逐一跑通或跑死
（akshare **1.18.91**，与上表同版本）。状态列的更新见上表，三个结论：

1. **债券源已补齐（本轮净收益）**：ETF 价格双轨的两轨都有源，且**都绕开已挂的东财通道**——
   国内 `fund_etf_hist_sina('sh511010')` ✅ **3,266 行**（新浪通道），
   海外 `stock_us_daily('TLT')` ✅ **2,668 行**（美股通道）。
   配合 `bond_zh_us_rate` 的收益率轨，§3.4 的「价格 + 收益率双轨」**素材已齐，可直接实现**。
2. **离岸人民币 CNH 是死路（已穷举）**：akshare 内所有可能通道全部失败——
   `forex_hist_em`（东财挂）、`fx_spot_quote` / `fx_pair_quote`（25 + 16 对报价里**没有 CNH**）、
   `fx_quote_baidu`（百度风控 `hit risk`，0 行）、`currency_pair_map`（investing.com 超时）、
   `currency_latest/history/time_series`（**签名要求 `api_key`**，违背「不新增密钥」）。
   ⇒ **离岸口径取不到。** 若必须展示，只能用 `currency_boc_sina('美元')` 的**在岸**中行牌价 / 央行中间价，
   且**必须标注口径是在岸**（与离岸 CNH 有点差，方向通常一致，但不是同一个价格）。
3. **比特币历史是死路**：`crypto_js_spot` 只有 10 行实时快照；另两个 `crypto_bitcoin_*`
   分别是 CME 合约成交 / 持仓（5 行）与上市公司持币报告（59 行），**均非价格时间序列**。
   ⇒ 比特币只能做「当日涨跌」，分位 / 均线一律做不了。

## 3. 口径统一（文章 1 的三个坑 + 对策）

文章作者一周内踩的三个坑，**我们必须提前写死规则**，否则同样会「数据天天出错」：

### 3.1 坑一：不同市场收盘时间不同 → 观测日不一致

**对策**：每个资产落库携带**两个时间**，前端按「数据截止」展示而非假装同一日。

- `trade_date` —— 该资产的**所属交易日**（A股/港股/美股各按本地交易所日历）
- `data_asof` —— 该值**实际取到的时刻**（北京时间）

界面右上角统一显示「**各市场数据截止：A股 15:00 / 港股 16:00 / 美股 05:00（北京）**」，
**永不显示一个笼统的「今日」**。这是文章里「同一天的数据没法同时获取」的正解。

### 3.2 坑二：24 小时交易品种的「收盘价」需人为定义

黄金 / 白银 / 原油 / 比特币几乎 24 小时交易，不同人取不同时点会得出**涨跌相反**的结论
（文章原文：「国内是下跌 2% 左右，海外是上涨 3% 左右」）。

**对策（硬规则，写进 spec）**：

1. 24h 品种的「当日收盘」**统一定义为北京时间 08:00 的快照**（可配置，配置项入 `.env`）；
2. 该口径**必须出现在界面的字段 tooltip 上**，不允许只写「收盘」；
3. 同一资产**只用一个源**，禁止 A 处用国内、B 处用海外——文章里的错乱正是多源混用导致。

**实测差异（2026-09-12）**：国内（上期所 / INE 主力连续）与海外（美股 ETF 代理）在
**近 12 个重叠交易日**的当日涨跌幅，**符号相反**的比例：

| 品种 | 国内源 | 海外源 | 符号相反 |
|------|--------|--------|---------|
| 黄金 | `futures_main_sina('AU0')` | `stock_us_daily('GLD')` | **8 / 12（67%）** |
| 白银 | `futures_main_sina('AG0')` | `stock_us_daily('SLV')` | **9 / 12（75%）** |
| 原油 | `futures_main_sina('SC0')` | `stock_us_daily('USO')` | **5 / 12（42%）** |

**这不是数值级差异，是结论级差异**——「今天黄金是涨是跌」这个结论会因此翻转，
正是文章作者遇到的「国内 −2%、海外 +3%」。

**决策（已定）：商品用国内口径。** 理由是可操作性——境内投资者实际能买的是沪金 / 沪银 / 原油，
「和我能买的东西对得上」比「和 COMEX 报价对得上」更重要。但**必须在卡片上标注「国内口径 · 含夜盘」**，
否则用户拿它对照国际金价会觉得数据错了。海外口径保留为**对照值**，不参与主结论。

### 3.3 坑三：估值口径无法跨资产复用

文章结论：**只有 A 股指数能给出可靠估值**，海外各国差异大、商品/加密根本没有盈利现金流可估。

**对策（与我们 L3 分层完全一致）**：

| 资产类别 | 「相对位置」指标 | 依据 |
|---------|----------------|------|
| A 股指数 | **估值分位**（PE / 股息率的历史分位） | 已有 `index_valuations` 表与岗位；自算链路见 `SelfCalcFetcher` |
| 港股 / 海外股指 | **价格分位**（现价在 N 年区间的百分位），**不做估值** | 无口径统一的数据源 |
| 债券 | **收益率分位** + 期限利差 | `bond_zh_us_rate` 自带 10Y-2Y 利差列 |
| 商品 / 加密 | **价格分位**，并标注「不产生现金流，分位≠贵贱」 | 与文章作者判断一致 |

> **口径参考（来自文章原文）**：作者的 A 股估值在试过多种算法后，最终采用「**中位数等权**」——
> 因为它比均值更能描述国内市场的真实状态；而海外各国差异大、单一算法不通用，商品与加密
> 更没有盈利 / 现金流可估。
>
> 我们的对策：**优先用中证官方口径**（`index_valuations` 的 PE / 股息率，可追溯、免自算争议），
> 把「中位数等权」作为**官方口径覆盖不足时的备选**（`SelfCalcFetcher` 已有自算链路）。
> 两条链路的口径**必须在界面上区分来源并标注**，禁止混算或互相覆盖。

### 3.4 债券：价格与收益率**双轨显示**（已定）

原文的债券 2 是 **国债ETF / 美债ETF**（看**价格涨跌**），而我们实测可用的 `bond_zh_us_rate`
给的是**收益率**（含中美 2 / 5 / 10 / 30Y）。**二者方向相反**：收益率上行 = 债券价格下跌。

**决策：两条都显示**，不做二选一——因为在这个模块里，**「反向」本身就是信号**。
原文说「哪天股债一起涨或一起跌，就值得多留意」，而只有同时看到价格与收益率，才能判断这件事。

| 轨道 | 展示内容 | 数据源 |
|------|---------|--------|
| **价格轨** | ETF 当日涨跌幅（走涨红跌绿） | 美债 `stock_us_daily('TLT')`（同 GLD 通道，待补测）；国内国债 ETF **需换源**（东财通道已挂，见 `#1431`） |
| **收益率轨** | 10Y 收益率及其日变动（bp） | `bond_zh_us_rate` ✅ 实测 717 行 |

**展示纪律**：收益率轨**不得套用涨红跌绿**——收益率上行并不代表「好」，
它与价格轨的涨跌语义相反，用同一配色会直接误导。收益率用**中性文字色**
（`--text-secondary`）+ 明确标注「上行 / 下行 N bp」。

## 4. 数据分层与落盘（避免重犯「全量堆砌」）

严格按 `data-strategy.md` L1~L4 判定：

| 数据 | 层 | 落盘？ | 量级估算 | 依据 |
|------|----|-------|---------|------|
| 20 资产**日频快照**（当日涨跌 + 分位） | L3 缓存 | **落**（唯一新增） | **15 行/天 ⇒ ≈5,500 行/年**（2026-09-13 实测口径：14 个可取数资产 + 1 条债券收益率轨；6 个结构性软占位不入库，见 §7） | 要算「异动」与回看历史，必须有序列；量级可忽略 |
| 指数 / 债券 / 商品**日线** | L3 缓存 | 落，**复用 `index_daily`**（现有 10 个万得系指数，单指数 2,104 ~ 6,466 行、最长 ≈26 年） | 每资产 +1 行/天 | 均线、分位都从此表算 |
| 全市场个股日线（做市场宽度用） | — | **暂不落** | 5,000 只 × 250 天 = **125 万行/年** | 这是唯一会「堆量」的部分，**按 L3 降级处理**（见 §5.3） |
| 实时行情 | — | **不落** | — | 前端直连，见 `realtime-data-sources.md` |

> 关键判断：**探市页的数据量根本不是问题**（一年 7,300 行）。
> 真正会失控的是「市场宽度」（需全部个股日线），因此它是**唯一被明确降级**的部分。

## 5. 温度计算法

### 5.1 截图给定的口径

从截图文字可确定的规则：

- **底座**：全A等权指数的 **500 日线**，输出「深温」值（示例 `-1.685`）
- **输出**：温度 **4 ℃**（示例），量纲 0~10
- **分区**：绿区 `(-10, -5]` / 黄区 `(-5, 0]` / 红区 `(0, 10]`——注意分区是在**偏离量**轴上，不是温度轴上
- **分布**：10 个档位的标的数量直方图（原文示例 `26 7 31 5 2 0 5 3 2` —— 仅 9 档可见，第 10 档原文未给出）
- **市场宽度**：`0.08%` 处于 200 日均线以上、`8.67%` 处于 500 日均线以上
- **发布节奏**：每日 **20:00**

### 5.2 我们的实现路径

```
全A等权指数日线（index_daily）
      ↓
偏离量 dev = (close - MA500) / MA500        # 文章示例 -1.685 量级
      ↓
截面 z-score / 历史分位归一
      ↓
温度 = clip(映射(分位) , 0, 10)              # 0~10 ℃
      ↓
落入 绿/黄/红 区  →  前端三色 token（复 temperature-redesign.md §5.1）
```

**「全A等权」怎么来（需拍板）**：

| 方案 | 数据 | 代价 |
|------|------|------|
| A. 用现成指数近似 | `sh000002`（A股指数，**8,724 行 ≈ 35 年**）或 `sz399317`（国证A指，5,248 行） | 零成本，**但不是等权**，与截图的「等权」口径不一致 |
| B. 自算等权 | 需全部 A 股日线 ⇒ 125 万行/年 | 重，且正是 §4 要避免的堆量 |
| C. 用韭圈儿已有的万得系指数 | `index_daily` 里已有 `881001.WI`（万得全A，6,465 行） | 零成本，非等权 |

**建议 A/C**：先用 `881001.WI` + `sh000002` 各算一条，与截图数值对照，确认口径接近后再定。
**不为了「等权」二字去拉全市场个股。**

### 5.3 市场宽度（`% 处于 200 / 500 日均线以上`）——唯一的重活

截图里 `8.67%` 这类数字需要**全部个股**的日线。三条路：

1. **降级**：只做**指数级**宽度（如「28 个申万一级行业中，几个在 200 日线上方」）——行业指数数量少、`index_daily` 可承载；
2. **借用 baostock**（已在依赖里）：批量拉全 A 日线，但需评估落盘量（125 万行/年）；
3. **不做**：该指标缺失时显示 `—`（符合 L3 可降级）。

**建议先 1，把 2/3 作为后续选项。** 这也是全篇最该由你拍板的取舍。

### 5.4 A 股焦点区：估值条 + 强度条

原文把两个指标嵌在 A 股区域底部、每日更新（此前只在周六「市场体检」发布）：

| 指标 | 视觉 | 口径 |
|------|------|------|
| **估值** | 一条横条，**绿 → 红** | **全指近 10 年的 PE 百分位**；靠左 = 便宜，靠右 = 贵 |
| **强度** | 一条横条，**灰 → 紫** | 市场动能：偏灰 = 情绪低迷，偏紫 = 交易活跃、趋势较强 |

- 常态下两条都停在中间，说明「一切正常」；
- 只有跑到**两端**（极度低估 / 极度超涨）才值得多想一步。

**我们需要注意两点**：

1. **估值条的绿→红与温度三色同源**（低=绿、高=红），应复用同一套 token，不要再造一套；
2. **强度条的灰→紫是第三套配色**，与涨跌、温度都不同。项目 tokens 里已有可对应项
   （`--color-primary` = `#7a7fa8` 灰紫蓝、`--tag-thistle`、`--tag-periwinkle`），
   但 `design.md` 正文未收录这三者——落地时**以 `frontend/src/style/colors.css` 为准**，
   并顺手补文档。

### 5.5 ⚡ 异动标记：双线规则（原文给出的完整逻辑）

这是全篇**唯一一条能直接抄下来的判定规则**——原文用「量血压」类比讲得很清楚：

```
线 1（相对 / 自适应）：与「自身过去一年」的波动比
        —— 过去一年这个资产通常波动多大？今天偏离了多少？
线 2（绝对 / 兜底）：   单日涨跌幅超过某个绝对阈值

任一触发 → 亮 ⚡
```

原文的例子（可直接作为我们实现的验收样例）：

| 资产 | 当日涨跌 | 平时日波动 | 是否亮 ⚡ | 原因 |
|------|---------|-----------|----------|------|
| 原油 | +3.5% | ≈2% | **是** | 超出自身上限，且绝对幅度大 |
| 日经225 | ≈+2% | 高于原油 | 否 | 对**它自己**来说不算罕见 |

> 关键点：**阈值是按资产自适应的，不是一个全市场统一的百分比**。
> 这与 `data-strategy.md` 的 L3 口径一致——只要「过去一年的该资产日收益序列」，
> 就能算（我们的 `index_daily` 已有足够历史）。

**实现提示**：线 1 用滚动波动率（如过去 250 个交易日日收益标准差）的倍数判定，
线 2 用固定绝对阈值兜底（对应原文「不管平时血压多高，量到 200 都不正常」）。
**两个参数都必须可配置**，并在事件底部附一句新闻事实解释（原文的「异动解读」）。

> **已落地（2026-09-13，#1461 止血批次）**：双线判定已在 `market_service.py` 实现，
> 参数为模块级常量——`ANOMALY_SIGMA_WINDOW=250`（滚动窗口）/ `ANOMALY_SIGMA_MULTIPLE=2.5`
> （线 1 倍数）/ `ANOMALY_ABS_THRESHOLD=3.0`（线 2 绝对阈值，%）/ `ANOMALY_MIN_SAMPLES=60`
> （样本不足则不判线 1，避免新股/短历史误报）。**当前是常量而非 env**——按
> `data-strategy.md`「字段级准入」精神，参数在无真实调参需求前不引入配置面；
> 若将来要按资产分档，再改为配置。

### 5.6 跨类对比：「谁和谁不一样」

原文反复强调的一句设计原则：

> 「最值得关注的不是谁涨谁跌，而是**谁和谁不一样**。」
> 「不看全局，单个资产的涨跌很难判断它到底意味着什么。」

由此得出三条**必须由界面承载**的对比，而不是把 20 个数字平铺给用户：

| 对比 | 含义 |
|------|------|
| **红多还是绿多** | 全球资金昨天在进攻还是防守（原文「看颜色大势」，不用读数字） |
| **有没有 ⚡** | 哪里发生了不寻常的事（§5.5） |
| **跨类关系** | 股涨债跌？股跌金涨？**股债一起跌**？——关系比单个数字信息量大 |

> 第 3 条是探市页真正的产品价值所在，也是「记账软件」与「行情软件」的分界：
> 我们不只是给数字，而是给**关系**。界面必须显式承载它（见 §6.1 的「跨类对照条」）。

## 6. 界面设计

### 6.1 探市页：在既有页面内新增「大类资产观察」区块

**不是新建页面**（见 §0.1）。既有 `views/explore/index.vue` 的顺序是：
`MarketHeader` → 温度仪表盘 → 转化区 → `ExploreAddSection` → `ExploreWatchlistTable` → `PageFooter`。

新增大类资产区块的落点与形态：

- **位置**：紧接 `ExploreTemperatureDashboard` 之后、转化区之前——温度回答「贵不贵」，
  大类资产回答「昨天发生了什么」，两者构成「先看全局、再看温度」的完整动线；
- **文件**：`views/explore/components/ExploreAssetOverview.vue`（与既有 `Explore*` 组件同级同名），
  内部按 6 组拆分或用数据驱动渲染，**不要写 20 份重复模板**；
- **必须复用**：`SectionHeader` / `MetricGrid` / `MetricCard`（既有温度仪表盘已在用）、
  `TemperatureGaugeCard`（`size: 'sm'` 即「探市页入口卡」）；页面级 `MarketHeader` / `PageFooter` 不动。
- **不要新造页头页脚**：探市与温度计共用同一套品牌头尾是既有约定。

区块内部布局要点：

1. **一条「市场状态条」**：各市场数据截止时间（§3.1），不做成装饰；
2. **6 组 × 20 张资产卡片**（自适应网格）。卡片三段：名称 + 涨跌幅（**涨红跌绿**）+ 相对位置条；
3. **相对位置条**是核心：横向刻度 + 游标标出「当前值在历史区间 / 分位」的位置，
   绿/黄/红三区**复用温度 token**（`--temp-low/mid/high`，见 §6.4 缺口 ①）；
4. **⚡ 异动标记**：按 §5.5 的**双线规则**（**不是固定 2%**），命中即标；区块底部配一条「异动解读」（当日新闻事实）；
5. **A 股焦点区**：紧随 A 股 4 张卡片之后（§5.4）——估值条 + 强度条；
6. **软占位（缺数据资产）**：**置灰**（`--bg-soft` 底 + `--text-disabled` 字）、数值显示 `—`，
   卡片下方一行小字说明原因（如「缺源 · 新浪不覆盖欧股」）。**不做转圈、不留空白格**——
   占位要说清「为什么没有」，而不是让用户以为在加载；
7. **口径提示**：24h 商品卡片带口径 tooltip（§3.2）；债券区按 §3.4 双轨标注。

### 6.2 温度计页

在已有 `temperature-redesign.md` 的视觉方案上，补三块：

1. **大号温度数字**（`4 ℃`）+ 绿/黄/红三色背景，字号最大，作为页面唯一锚点；
2. **三区刻度尺**：`-10 … -5 … 0 … 10`，游标标出当前偏离量，区带用绿/黄/红；
3. **温度分布直方图**：10 档位的标的数量条；**需注明分母**（是「全市场个股」还是「指数/行业」——取决于 §5.3 的取舍）；
4. **市场宽度两个数字**（200 日 / 500 日线上方占比）+ 「发布 20:00」时间戳。

### 6.3 两种消费形态：页面 + 邮件简报

文章最终交付的不是可交互页面，而是**每天早上推的一张图**（文末即「每日简报」）。
我们支持两种形态，**共用同一份数据**：

| 形态 | 面向 | 投递方式 | 对口径的要求 |
|------|------|---------|-------------|
| **页面**（探市 / 温度计） | 主动查看 | Web | tooltip 里写清口径即可（§3.1 / §3.2） |
| **每日简报** | 被动接收 | **邮件（订阅制）** | **必须把口径写进正文**——邮件里没有 tooltip |

> 简报形态对口径的要求**比页面更严**。文章里「每天都要解释数据为什么和别处不一样」的根因，
> 正是简报正文没有承载口径。简报正文固定带一行：
> 「A 股截止 15:00 / 港股 16:00 / 美股 05:00（北京）；商品与加密按北京 08:00 快照、海外算法」。

#### 6.3.1 现状：仓库里没有任何发信能力

全仓检索确认：`pyproject.toml` 与 `backend/app/` **零邮件依赖**
（`email` 的命中全部是 Supabase 登录鉴权，不是外发邮件）。因此这一步是**从零选型**，不是复用。

| 方案 | 免费额度 | 取舍 |
|------|---------|------|
| 腾讯云 / 阿里云邮件推送 | 有免费额度 | 国内到达率好，需实名与域名 |
| Resend / Brevo | 免费层较宽 | 海外服务，国内到达率需实测 |
| 个人邮箱 SMTP（QQ / 163 等） | 免费 | 最快跑通，但**易被判垃圾、有日限额**，只适合自测 |
| 暂不发信，先出 HTML | — | 最保守：先做好渲染，投递后置 |

**建议路径**：先用「个人 SMTP / 控制台输出 HTML」把**渲染**跑通（即下面的开发者口子），
投递通道留到订阅功能真正要做时再定。

#### 6.3.2 订阅者来源：复用 `users.email`，不新建用户表

`users` 表已有 `email`（由 Supabase 托管）与 `is_active`。订阅只需**新增一个订阅状态**，
不需要用户表、不需要重复收集邮箱。

> **跨域红线**：`users` 属 **user 域**（Supabase Postgres），而行情 / 简报数据属 **market 域**（Turso / SQLite）。
> 项目硬规则是「**跨域零外键**」——订阅状态若要落库，**必须落在 user 域**（与 users 同库），
> 或在 market 域只存邮箱快照 + 显式时间戳，**禁止外键指向 `users.id`**。

#### 6.3.3 邮件渲染约束（邮件 ≠ Web，形态取舍的关键）

邮件客户端的能力远低于浏览器，直接决定「简报长什么样」：

| 约束 | 后果 |
|------|------|
| 无 JavaScript | 任何交互、动态计算都做不了——**数据必须服务端渲染完** |
| 外部 CSS 支持差 | 样式必须**内联**，布局退回**表格**或受限 flex |
| 图片默认可能被屏蔽 | 「一张图」形态会**直接白屏**——文章作者的图在邮件里不可靠 |
| 深色模式不可控 | 客户端可能强制反色，**暖奶油白底 / 深色文字未必成立** |
| 客户端差异 | 同一封邮件在不同客户端渲染差异大 |

**结论（建议）**：简报以 **HTML 邮件为主**（内联样式 + 表格布局 + 系统字体栈），
把「一张图」作为**可选附件**而非唯一载体。这样图片被屏蔽时，信息仍然完整可读。
—— 这与文章的「一张图」形态**刻意不同**，理由就是邮件会比 Web 更早失效。

#### 6.3.4 开发者作为第一个订阅者（本期先留的口子）

不先做订阅管理，先做**一个可重复执行的 CLI 入口**（`app/tools/` 已有 `scheduler.py` 等先例）：

```
pdm run brief --to <你的邮箱>            # 渲染并发出当日简报
pdm run brief --dry-run --out brief.html # 只渲染到本地文件，不发信（零依赖、零风险）
```

实现次序建议：

1. **先只做 `--dry-run --out`**：渲染 HTML 到本地，人工在浏览器 + 邮件客户端（把 HTML 当附件发给自己）各看一遍；
2. 确认渲染过关后，再加 `--to`：接 SMTP 把同一份 HTML 发出去；
3. 只有 1~2 都稳定，才做「订阅开关 + 退订」。

**这样你自己就是第一个订阅者，且第 1 步不依赖任何邮件服务选型。**

#### 6.3.5 退订与合规

- **必须可退订**：一旦对外发信，退订入口与退订生效是硬要求；
- **免责声明**：沿用 `temperature-redesign.md` §5.3 的措辞纪律——
  简报中**不得出现荐股 / 跟单 / 预测未来涨跌**的表述；温度低 ≠ 买入信号；
- **口径声明**：见本节开头的固定口径行。

### 6.4 与项目设计语言的强制对齐（落地前置约束）

本文第 6 节的两张界面稿是**结构性草案**，用通用色绘制。**真正落地时必须全站统一**，
以以下四者为权威（优先级从高到低）：

| 权威来源 | 管什么 |
|---------|--------|
| `frontend/src/style/colors.css` + `design-tokens.css` | **实际 token 值**（唯一真相源，250 行） |
| `frontend/design.md` | 亮色设计语言（色阶语义、字阶、间距、形状、组件） |
| `frontend/design.dark.md` | 暗色适配（含 HSL 动态计算规则） |
| `docs/design/`（`brand-v1.7.md` / `components.md` / `font-design.md`） | 品牌、组件、字体细则 |

#### 6.4.1 视觉元素 → token 映射

| 界面元素 | 项目 token | 备注 |
|---------|-----------|------|
| 页面底 / 卡片底 | `--bg-page` `#FDFBF7` / `--bg-card` `#FFFFFF` | 暖奶油白体系，**不是纯冷白** |
| 涨 / 跌 | `--color-rise` `#E34F38` / `--color-fall` `#7BC49A` | **禁止直接用 `--brand-*`**（编码红线） |
| 颜色深浅 = 幅度 | 由 `--color-rise`/`--color-fall` 按幅度取色阶 | 对应原文「颜色越深幅度越大」 |
| 温度三色（探市位条 / 温度计） | `--temp-low` / `--temp-mid` / `--temp-high` | 见 6.4.2 缺口 ① |
| 强度条（灰→紫） | `--color-primary` `#7a7fa8` ／ `--tag-thistle` ／ `--tag-periwinkle` | 见 6.4.2 缺口 ② |
| 20 资产分组标识 | `--asset-stock` / `--asset-bond` / `--asset-etf` / `--asset-crypto` / `--asset-saving` | **已有资产类别色，直接复用** |
| 主要 / 次要 / 辅助文字 | `--text-primary` `#2D2A24` ／ `--text-secondary` `#6B655C` ／ `--text-tertiary` `#8C8478` | 暖灰体系 |
| 边框 / 卡片内分割 | `--border-default` `#E5DDD4` ／ `--border-subtle` | |
| 卡片圆角 / 内边距 | `--radius-md` 12px ／ `--space-standard` 24px | 大卡片 `--radius-lg` 16px + `--space-loose` 32px |
| 数据密集区 | `--space-compact` **16px**、行高锁 **40~44px** | 强制紧凑原则 |
| 数字 | Inter（正文）／ **SF Mono / JetBrains Mono + `tabular-nums`** | 金额与涨跌幅**必须等宽对齐** |

**两条硬约束**（`design.md` 明文）：

1. **正负符号与数字同色**：`+` 同涨色、`-` 同跌色，禁止只改数字色而符号保持默认；
2. **业务代码禁止直接调用 `--brand-*`**：涨跌一律走 `--color-rise` / `--color-fall`，
   以保证暗色模式全站自动同步。

#### 6.4.2 三个必须先补的缺口（否则落地即返工）

**① `--temp-*` 温度三色当前并不存在。**
`temperature-redesign.md` §5.1 规定了 `--temp-low` / `--temp-mid` / `--temp-high`
并注明「写入 `colors.css`」，但**实测 `colors.css` 里没有这三个变量**——
也就是说那份文档是「计划」，尚未落地。而它开头写的正是
「为彻底修复 **undefined 变量 bug**」。**探市页与温度计都要用温度三色，
必须先落 token，再写页面**，否则就是第三次踩同一个坑。

**② 「强度条 灰→紫」在 `design.md` 正文无记录。**
token 层有可对应项（`--color-primary` 灰紫蓝 / `--tag-thistle` / `--tag-periwinkle`），
但设计语言正文只覆盖品牌色、功能色、中性色三族。落地时应**以 `colors.css` 为准**，
并顺手把这族颜色补进 `design.md`，避免下一个人再找不到。

**③ 字阶下限是 13px，草案稿用了 11~12px。**
`design.md` 的字阶系统最小档是 `--text-label` 13px / 500，
**没有 11px / 12px 档**。因此草案稿里的辅助文字（如「价格分位 41%」）落地时
必须升到 `--text-label` 13px，或改用 `--text-small` 14px，
**不得**为了塞下信息而自造小字号。

#### 6.4.3 落地检查清单

- [ ] 温度三色 token 已写入 `colors.css`（缺口 ①）
- [ ] 强度条配色已确认并补进 `design.md`（缺口 ②）
- [ ] 全部字号 ≥ `--text-label` 13px（缺口 ③）
- [ ] 涨跌颜色全部经 `--color-rise` / `--color-fall`，无 `--brand-*` 直引
- [ ] 正负号与数字同色
- [ ] 所有金额 / 涨跌幅列加 `tabular-nums`
- [ ] 暗色模式（`design.dark.md`）下逐屏走查
- [ ] 探市页与温度计页面底部均带图例 + 免责声明（`temperature-redesign.md` §5.3）

## 7. 落库与 job 规划

> **2026-09-13 实施回填（#1460 方案 B）**：本节原为规划口径，第一批已落地——`market_snapshot`
> job + `/api/market/overview` 读库路径 + 本机调度接入。实施计划与验收标准见
> `docs/working-notes/market-snapshot-persist-plan-2026-09-13.md`；决策见 `decisions.md`
> 2026-09-13 行（D23）。下表「状态」列区分**已落地**与**仍规划**。

| 新增/复用 | 内容 | 频率 | 状态 | 说明 |
|-----------|------|------|------|------|
| **复用** `market_multi_items` | 探市快照：**14 个可取数资产 + 1 条债券收益率轨 = 15 行/天** | 每日 08:00 | ✅ 已落地 | `source='market_snapshot'`、`item_type='asset'`（债券轨 `item_type='bond_yield'` / `item_code='cn_us_10y'`）。**6 个结构性软占位不入库**（海外 4 + 中证2000 + 比特币，由 `ASSET_CONFIG` 常量提供）。**不新建表**——字段与唯一键 `(source,item_type,item_code,collected_at)` 直接够用，保留期复用既有 1 年清理 |
| **新增** job `market_snapshot` | 每日取数 → 落库 | 每日 08:00 | ✅ 已落地 | `app/services/sync/jobs/market_snapshot_job.py`；已注册进 `DataSyncOrchestrator` 并加入 `execution_plan`；接入本机调度（`calendar='none'`，A 股假期照常跑，见 §7.2） |
| **新增** 读库路径 + 来源开关 | `/api/market/overview` 优先读库，库空/异常回退实时取数 | — | ✅ 已落地 | 开关 `MARKET_OVERVIEW_SOURCE`：`live`（**默认**）/ `db`，非法值回退 `live`。响应体新增 `data_source` 字段（前端不消费，便于排查） |
| 复用 `index_daily` | 补交易所指数（沪深300 / 中证500 / 恒生 / 美股 / 债券） | 每日 | ⬜ 未做 | 现有 `index_daily_job` 走韭圈儿；交易所指数需经 akshare 通道补入同表（模型注释已预留此路径） |
| 复用 `market_composites` | 温度合成结果 | 每日 20:00 | ⬜ 未做 | `self_calc` source 已有先例（8 行） |
| 复用 `index_valuations` | 中证官方估值 | 每日 | ⬜ 未做 | **当前 0 行，需先把回填跑通** |
| 挂到 `scheduler` | 两条时点（见下） | — | ✅ 探市已挂 08:00 | 遵循 #1400 抓取礼仪（jitter） |

> **两个不同的发布时点**（来自两份材料的实际做法）：
>
> - **探市页 / 每日简报 → 早间**（文章作者取「早上 7 点」）：必须等美股、欧股收盘，
>   才能拿到隔夜海外数据；这正是文章里「7 点取还是取不到、得用实时数据补」的原因。
>   **建议推迟到 08:00**，与 §3.2 的 24h 品种快照时点对齐，一条时间线搞定，不必两套。
>   —— **已按 08:00 落地**（`SCHEDULER_MARKET_CRON`，默认 `0 8 * * *`）。
> - **温度计 → 晚间 20:00**（截图明确「发布于 20:00」）：A 股收盘后即可算，不依赖海外。
>
> 两条时点互不冲突，可在同一个 `scheduler` 入口下用不同参数触发。

> 按 `data-strategy.md` §3「数据准入四问」，上表每一项在实现 PR 里都须补「谁在用 / 什么场景 /
> 缺了怎样 / 成本多大」四答；本表已给出第 4 问的量级。**探市快照的四答**（#1460 实施时留痕）：
> 谁在用 = 探市页「大类资产观察」区块 / 温度计页；什么场景 = 打开页面看当日涨跌 + 分位 +
> 异动；缺了怎样 = 页面卡片只剩 `—`（源不可达时），或长期展示陈旧数据（F6）；成本多大 =
> **15 行/天 ⇒ 约 5,500 行/年**（远小于 `daily_worth` 的 750 万行）。

### 7.1 读库口径与「坏数据可见」（2026-09-13 实施）

- **只落结果、不落序列**：分位 / σ 在取数时由源返回的全历史当场现算，库内**不存收盘序列**
  （L3 口径；实测 12/14 资产 1492~8724 行，落序列既无必要也违背「不存多余数据」）。
- **单资产失败落 `stale=true`，不落上次的值**：读路径遇到最新一行是 `stale` 时，**回退**到
  最近一条非 `stale` 行并给该行打 `stale=True`——「今天失败」这件事本身可审计，而页面仍有值。
- **交易日滞后守卫**（`STALE_TRADE_DAYS=14`）：某资产 `trade_date` 落后**同批次最大值**超过
  14 天，即使源没报错也置 `stale=true`。这是 F6 那类事故的兜底，不依赖人工巡检。
- **F6 根因（已修）**：`akshare.currency_boc_sina` 的**默认日期区间被上游硬编码**为
  `20230304`~`20231110`（`inspect.signature` 实证），调用点不传 `start_date`/`end_date`
  就永远只拿到 2023 年那一段。修复方式：`ASSET_CONFIG` 汇率两段加 `date_range_years: 3`，
  由 `_resolve_source_call()` 注入显式区间；实测显式传参返回 **890 行、最新到 2026-09-12**。
  结论：**不是数据源坏了，是调用点漏传日期**——故原计划的「降级软占位」作废，改为直接修源。

### 7.2 交易日口径：探市资产不受 A 股日历约束（2026-09-13 实施）

本机调度原按「A 股休市则全局跳过」处理，但探市 20 资产含**美股 / 商品 / 汇率**——
「A 股休市」不等于「全球休市」，一刀切会让这些卡片在国庆期间**停更一周**。
故 `DailyJobSpec` 新增 `calendar` 字段：

| 取值 | 含义 | 用于 |
|------|------|------|
| `cn` | 受 A 股日历约束（周末 / 法定节假日 / 调休补班周末跳过） | `temperature` / `fund_nav` |
| `none` | 不受任何交易日历约束（7×24） | `market_snapshot` |

判定函数 `is_job_rest_day(spec)` 只对 `calendar='cn'` 的任务调 A 股日历；
启动补跑（`catch_up_targets`）同样**逐任务**判定，不再有全局早退。

## 8. 待拍板 / 已定

### 8.1 已定（2026-09-12，不再需要拍板）

| 事项 | 决策 | 依据 |
|------|------|------|
| 海外 4 资产缺源 | **软占位**：置灰 + `—` + 原因小字，**不阻塞本期** | 用户决策；缺口跟踪 `#1436`。缺源的是 §2.2 序号 9~12（欧股 3 个 + 日股 1 个，共 **4 个**）——若将来决定砍掉，口径是 **20 → 16**，不是 18 |
| 商品涨跌口径 | **国内口径**（沪金/沪银/INE 原油主力），标注「含夜盘」；海外仅作对照 | 实测符号相反率 67% / 75% / 42%（§3.2）——结论级差异，且国内口径更可操作 |
| 债券 | **价格与收益率双轨显示** | 反向本身即信号（§3.4）；收益率轨**不套用涨红跌绿** |
| 优先级 | **先做探市新功能**，数据底座后置 | 用户决策；底座跟踪 `#1434`（调度失败）/ `#1431`（东财通道） |
| 封面形态 | **改造既有探市页**，新增「大类资产观察」区块；**不新建页面** | 实测 `views/explore/` 已存在（§0.1） |
| 异动判定 | 按 **⚡ 双线规则**（自身波动率 + 绝对阈值），**非固定百分比** | 原文完整逻辑（§5.5） |

### 8.2 仍待拍板

| # | 事项 | 选项 |
|---|------|------|
| 1 | 「全A等权」用什么底座 | 现成指数近似（`881001.WI` / `sh000002`）／自算等权（125 万行/年） |
| 2 | 市场宽度的分母 | **等用户调研**；注意最接近的既有卡 `#1407` 是「指数估值分位」，与本项口径不同（§0.1.1 注） |
| 3 | 24h 品种的「收盘」时点 | 北京时间 08:00（建议）／ 其他 |
| 6 | 比特币只有实时无历史 | 只展示当日涨跌（不做分位）／ 另找历史源 |
| 8 | 简报投递通道 | 腾讯云 / 阿里云邮件推送 ／ Resend / Brevo ／ 个人 SMTP（仅自测）／ 先只出 HTML 不发信（**建议**，见 §6.3.1） |
| 9 | 简报形态 | **HTML 邮件为主 + 可选图片附件（建议，因邮件会屏蔽图片）** ／ 纯图片 ／ 纯文本 |
| 10 | 订阅功能的范围 | 本期只做「开发者自己的口子」（`pdm run brief --to`）／ 同时做订阅开关 + 退订 |
| 11 | ⚡ 双线参数 | ✅ **已定并实现**（2026-09-13，#1461）：波动率窗口 **250 日** / 倍数 **2.5** / 绝对兜底 **3.0%**，样本下限 60——详见 §5.5。**不再是待拍板项** |
| 12 | 国内国债 ETF 的源 | 东财通道已挂（`#1431`），需换源或改用其他通道 |
